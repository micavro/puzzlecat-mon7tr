# Solve log — 动物躲猫猫

- Puzzle ID: `BULEpJHi`
- Owner: `codex-sub-m-20260712`
- Status: correct
- Incorrect submissions: 0

## Observations and reasoning

- Claimed and archived under the shared worker protocol; beginning from local files.
- Public factual/mechanism research is permitted, but no direct answer, official solution, discussion answer, or reproduced walkthrough will be used. Any final answer must be independently derived from the puzzle statement.
- Mechanism from examples: an animal drops its `犭` radical, leaving a component hidden as a full character. Take the initial of the character immediately left of that component and the final plus tone of the character immediately right. Example `树 苗 长` hides `猫 -> 苗`: `sh` + `ang` (tone 3) = `shǎng`; `一 句 名` hides `狗 -> 句`: `y` + `ing` (tone 2) = `yíng`.
- 1. `死 者 搬`: `猪 -> 者`; `s` + `an` (搬 tone 1) = `sān` = 三.
- 2. `个 瓜 驼`: `狐 -> 瓜`; `g` + `uo` (驼 tone 2) = `guó` = 国.
- 3. `银 星 闪`: `猩 -> 星`; `y` + `an` (闪 tone 3) = `yǎn` = 演 (not 眼 in the final phrase).
- 4. `严 师 第`: `狮 -> 师`; `y` + `i` (第 tone 4) = `yì` = 义.
- The four independently derived syllables spell `三国演义`.

## Candidate ranking

- 1. `三国演义` — exact four-character phrase from `sān guó yǎn yì`, with every line explained by the same radical-removal and pinyin-splicing rule.

## Submissions

- `三国演义` — correct (HTTP 200, first submission).

## Final classification

- `correct`, incorrect count 0, answer `三国演义`.

## Stopping criteria

- Correct on confirmed verdict; `cannot_do` if the evidence ceases to distinguish a candidate; hard stop at 20 incorrect submissions.
