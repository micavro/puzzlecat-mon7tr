# D3WPz728 - Animals in Grids

Owner: `chat2-root-20260712`

## Observations

- Seven English clues each name a traditional Chinese riddle-grid transformation and
  request an animal. Parentheses give extraction index/animal length.

## Reasoning

- Pond grass `REED`, rolled curtain/reversal -> `DEER`; take 2/4 = `E`.
- Mother's door key wordplay -> `MONKEY`; take 3/6 = `N`.
- Sink drain `CLOG`, shrimp-antenna combination -> `DOG`; take 3/3 = `G`.
- Book/tree part `LEAF`, going-downstairs rotation -> `FLEA`; take 2/4 = `L`.
- Editable webpage `WIKI`, double-hook half swap -> `KIWI`; take 4/4 = `I`.
- Stable clue/white-head wordplay -> `STALLION`; take 1/8 = `S`.
- `HOGWARTS` without S = `HOGWART`; swing half swap -> `WARTHOG`;
  take 5/7 = `H`.
- Extracted result: `ENGLISH`.

## Candidate ranking

- `ENGLISH` - all seven indexed rows agree.

## Submissions

- `ENGLISH` - correct (`0 / 20` incorrect).

## Stopping criteria

- Resolve correct if `ENGLISH` is accepted.

Resolved `correct` with answer `ENGLISH`.
