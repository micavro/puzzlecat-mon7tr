# bsb5uOJW - 降水

Owner: `chat2-root-20260712`

## Observations

- FPA puzzle; solved strictly from the archived image without answer/solution lookup.
- Three rebus rows show Chinese-character construction guides and pinyin-length boxes.
- Each pinyin row has one green extraction cell; the extracted Latin letters are
  `A`, `I`, and `N` in order.
- The final sprout/stem glyph is deliberately drawn as a lowercase `r` followed by
  three green slots.

## Reasoning

- Combine the drawn `R` with extracted `AIN` to obtain `RAIN`, matching the Chinese
  title `降水`.

## Candidate ranking

- `RAIN` - exact visual extraction and title confirmation.

## Submissions

- `RAIN` - correct (`0 / 20` incorrect).

## Stopping criteria

- Resolve correct if `RAIN` is accepted.

Resolved `correct` with answer `RAIN`.
