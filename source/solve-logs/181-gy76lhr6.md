# 探骊格之底线 solve log

- Puzzle ID: `gy76lhr6`
- Owner: `codex-sub-a-20260712`
- Status: correct and resolved
- Incorrect submissions: 2
- Submitted answers: `五行 土` (incorrect), `数字 十` (incorrect), `生肖 牛` (correct)

## Puzzle text

- Title: `探骊格之底线`
- Flavor: `好的探骊格和坏的探骊格（本部分与字谜无关）`
- Clue: `区别只是有无底线（2+1）`
- Tag: `探骊格`

## Observations

- 探骊格 requires the answer to contain both an inferred category (谜目) and its entry (谜底).
- The enumeration `(2+1)` therefore likely means a two-character category plus a one-character entry.
- The flavor explicitly says its comparison of good and bad 探骊格 is unrelated to the character riddle, so the operative wordplay is only `区别只是有无底线`.
- `底线` may indicate adding/removing a bottom horizontal stroke from a character. The clue likely defines or contrasts a pair of characters, while the requested answer names one under an inferred two-character category.

## Candidate ranking

1. `生肖 牛` - correct. In the full `2+1` answer `生肖牛`, the category's first character `生` differs from its entry `牛` only by the bottom horizontal stroke. `牛` is validly one of the twelve `生肖`. This makes the stated distinction internal to the complete 探骊 answer and uses both category and entry, unlike the rejected generic pair.
2. `五行 土` - rejected. The parse was `是` homophonically supplying `十`, then adding the bottom line to produce `土`, an entry under `五行`.
3. `数字 十` - rejected. This used the no-bottom-line side of the same generic `十/土` pair.

The public aggregate at archive time was 56 attempts for 28 solves, consistent with a deliberately tempting two-way `十/土` distinction. This supports treating `数字 十` as the main fallback rather than enumerating unrelated one-stroke pairs.

## Submission record

- `五行 土` - incorrect (`回答错误，请再试一次`), HTTP 200, prior attempts 0, submitted 2026-07-12T12:52:08Z. Incorrect count is now 1.
- `数字 十` - incorrect (`回答错误，请再试一次`), HTTP 200, prior attempts 1, submitted 2026-07-12T12:56:51Z. Incorrect count is now 2.
- `生肖 牛` - correct (`恭喜你，回答正确！`), HTTP 200, prior attempts 2, submitted 2026-07-12T13:01:18Z. Final incorrect count: 2.

## Stopping condition

- Stopped after the internally self-confirming answer `生肖 牛` was accepted.
