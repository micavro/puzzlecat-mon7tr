# Solve Log: zEbG691D

- Title: `来自地球的礼物`
- Draw: `tmp/practice-draw.json` (`2026-07-11T21:31:01.9377657Z`)
- Solver: child agent coordinated by root
- Submission limit: 20 incorrect answers
- Started: `2026-07-12` (Asia/Shanghai)

## Observations

- Puzzle and one resource archived locally.

## Attempts

1. `BUG NET` - incorrect (`1 / 20`).
2. `PRAGMATA` - intermediate: each gift corresponds to an Earth memory; final enumeration `(6 10 3)` or `(2 3)`.
3. `DIANAS COLLECTION REM` - incorrect (`2 / 20`).
4. `REMOTE CONTROLLED CAR` - correct.

## Grid Transcription

- Flavor: a girl on the Moon received 16 gifts from Earth, but only 15 appear; identify the missing gift.
- The image is a 10 by 10 orthogonal word-search grid. The instruction explicitly says `无斜向` (no diagonals).
- Grid, row-major:
  - `SBEFPSWING`
  - `AARLAKTENT`
  - `NSIORACPRG`
  - `DKFWATRBCL`
  - `CEPESEAURO`
  - `ATMROBYGTB`
  - `SBALLOONTE`
  - `TACAGANEVM`
  - `LLNUGRETAW`
  - `ELSLIDEATA`
- The category/length inventory is:
  - sports and amusement facilities, 5 gifts: `5, 5, 10, 10, (5 3)`
  - items commonly used indoors, 4 gifts: `5, 6, 7, (3 2)`
  - camping/travel, 3 gifts: `4, 7, 8`
  - nature/beach scenes, 2 gifts: `6, 10`
  - capturing tool, 1 gift: `6`

## Straight-Line Word Finds

- Sports/amusement: `SWING` (r1c6-r1c10), `SLIDE` (r10c3-r10c7), `BASKETBALL` (r1c2-r10c2), `SKATEBOARD` (r1c6-r10c6), `WATER GUN` (r9c10-r9c3, reversed).
- Indoor: `GLOBE` (r3c10-r7c10), `CRAYON` (r3c7-r8c7), `BALLOON` (r7c2-r7c8). The `(3 2)` entry remains unidentified.
- Camping/travel: `TENT` (r2c7-r2c10), `PARASOL` (r1c5-r7c5), `CAMPFIRE` (r8c3-r1c3, reversed).
- Nature/beach: `FLOWER` (r1c4-r6c4), `SANDCASTLE` (r1c1-r10c1).
- Capturing tool: `BASKET` (r1c2-r6c2). It overlaps the first six letters of `BASKETBALL`.
- These 14 finds exactly match the stated category counts and lengths apart from the single indoor `(3 2)` phrase.

## Fifteenth Gift and Missing Gift

- The missing indoor entry is `CRT TV`: column 9, rows 4 through 8 reads `C R T T V`, exactly matching `(3 2)`.
- Marking the 15 listed gifts leaves 13 unused cells. In normal row-major order they read `PRBUGAGEMATA`.
- A six-letter word not present in the category inventory runs down column 8, rows 4 through 9: `BUGNET`. Four of its letters (`B,U,G,E`) are among those 13 unused cells; `N` intersects `BALLOON` and `T` intersects `WATER GUN`.
- This is the flavor's missing sixteenth gift: `BUG NET`.
- After additionally marking `BUG NET`, exactly eight cells remain. In row-major order they are:
  - r3c8 `P`, r3c9 `R`
  - r8c4 `A`, r8c5 `G`, r8c10 `M`
  - r10c8 `A`, r10c9 `T`, r10c10 `A`
- They spell `PRAGMATA`, the title of Capcom's science-fiction game centered on Diana, a young girl on the Moon. This is a strong checksum for the complete word set and the flavor, not the item asked for.
- Final-answer candidate: `BUG NET` (normalization also accepts `BUGNET`). Confidence: `0.98`.
- Submission advice: send `BUG NET` to the coordinator for one controlled submission; this agent has not submitted anything.

## BUG NET Rejected; Correct Classification

- The coordinator submitted `BUG NET`; the server returned `incorrect`. Incorrect submissions are now `1 / 20`.
- This falsifies the interpretation above. The semantic category `捕捉工具` (capturing tool), length 6, naturally and unambiguously identifies `BUG NET`, not `BASKET`.
- `BASKET` is merely the first six letters of the already-counted `BASKETBALL`; treating that nested substring as a separate gift double-counted one placement and incorrectly promoted `BUG NET` to the missing item.
- The corrected set of 15 listed gifts therefore includes `BUG NET` at r4c8-r9c8. Marking those 15 words leaves exactly eight cells, whose row-major reading is directly `PRAGMATA`.
- The puzzle is tagged `English` and `Game`; `PRAGMATA` is an English game title, and its Moon setting plus the girl Diana independently confirm the flavor. No further transformation or localization is indicated.
- Revised final-answer candidate: `PRAGMATA`.
- Confidence: `0.99`. This is a direct exhaustive leftover extraction, and the only competing interpretation has now been server-falsified.
- Submission advice: send `PRAGMATA` to the coordinator for one controlled submission; this agent has not submitted it.

## Accepted Final Answer

- The 15 listed gifts are 15 of the 16 official `Read Earth Memory` (`REM`) collectibles in *PRAGMATA*.
- Comparing the exhaustive word-search list against the game's official 16-item REM list leaves only `RC Car` absent.
- The intermediate enumeration disambiguates its expanded name: `REMOTE` (6), `CONTROLLED` (10), `CAR` (3), or `RC CAR` (2, 3).
- `REMOTE CONTROLLED CAR` was accepted by the server. Final incorrect count: `2 / 20`.
- Evidence: `submissions/2026-07-11T22-01-29-465Z/submission.json`.
