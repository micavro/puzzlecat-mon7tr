# CROSS†WORD - solve log

- Puzzle ID: `7wAIxIpP`
- Owner: `codex-sub-b-20260712`
- Status: active
- Incorrect submissions: 0

## Source

- Flavor: *Stray* **Soul** *Around*
- Clues: `Artist of them`, `Beam`, `Change`, `Done`, `End`, `Fate`, `Group killing`.
- The image is a seven-entry barred crossword. Confirmed entry lengths are 4, 8, 3, 10, 4, 7, and 11.
- Numbered cells 1 through 8 provide an extraction.
- Correct-answer message is `Forever ~~~`.

## Grid geometry

- Left horizontal H4 crosses left vertical V8 at H4[3] = V8[2].
- Right horizontal H4 crosses central V10 at H4[0] = V10[3], and right V7 at H4[2] = V7[1].
- Main horizontal H11 crosses V8, V3, V10, and V7 at H11[4/6/8/10] = V8[5], V3[1], V10[7], V7[5].
- Numbered extraction: 1=H11[0], 2=V8[5], 3=V7[2], 4=V10[8], 5=right-H4[3], 6=V7[0], 7=V8[1], 8=V3[0].

## Working fills

- `Beam` = `RAY` (V3).
- `Done` = `OVER` (left H4) is plausible.
- `Fate` = `PREDICAMENT` (H11) was the unique strong thesaurus match for the 11-letter crossing pattern under one candidate fill.
- Other length-matched candidates: `Change` = `MUTATION` or `VARIANCE`; `End` = `PURPOSE`; `Group killing` = `MASSMURDER`; `Artist of them` may be an artist name or a four-letter thematic word.
- The flavor likely gives a cryptic/thematic operation involving `SOUL`, so direct synonyms alone may not be the final entries.

## Resolved mechanism

- Recounting the image gives the exact slot lengths 4, 8, 3, 10, 4, 7, 11.
- The clues admit matching answers such as `POET` (artist of words), `RAY` (beam), `TURNOVER` (change), `OVER` (done), `OUTCOME` (end), `CONSIGNMENT` (fate/lot), and `MASS MURDER` (group killing). The wording and flavor indicate that their letters may stray/rearrange around the grid rather than being entered conventionally.
- The numbered cells can then be selected from the appropriate answer multisets as:
  1. `G` from the 11-letter fate answer
  2. `E` from the 8-letter change answer
  3. `O` from the 7-letter end answer
  4. `M` from the 10-letter group-killing answer
  5. `E` from the four-letter artist answer
  6. `T` from the end answer
  7. `R` from the change answer
  8. `Y` from `RAY`
- Extraction: `GEOMETRY`. This also fits the geometric crossword layout. `GEOMETRY DASH` is a lower-ranked fuller-theme candidate, but only `GEOMETRY` is directly extracted.
- First submission candidate: `GEOMETRY`.

## Correction after first submission

- `GEOMETRY` was submitted at 2026-07-12T13:41:34Z and rejected. Incorrect count: 1.
- The direct, non-anagram fills explain the grid much more tightly:
  - `Group killing` = `GENOCIDE` (8)
  - `Beam` = `RAY` (3)
  - `Change` = `ALTERATION` (10)
  - `End` = `DESTINATION` (11)
  - `Fate` = `DESTINY` (7)
  - `Done` = `OVER` (4)
- Main-crossing checks are exact: DESTINATION[4/6/8/10] = I/A/I/N, matching GENOCIDE[5] = I, RAY[1] = A, ALTERATION[7] = I, and DESTINY[5] = N.
- Numbered cells give D-I-S-O-?-D-E-R. The right four-cell slot for `OVER` is forced by ALTERATION[3] = E and DESTINY[1] = E to display `E?E?`; using the remaining letters yields `EVER`, so numbered cell 5 is R. Extraction: `DISORDER`.
- This deliberate corrupt crossing is consistent with *Stray Soul Around* and the title's dagger. The correct-answer message `Forever ~~~` also calls out the resulting `EVER`.
- Next submission: `DISORDER`.

## Result

- `DISORDER` submitted at 2026-07-12T13:44:29Z: **correct**.
- Final incorrect count: 1 (`GEOMETRY`).
- Final answer: `DISORDER`.

## Stopping criteria

- Submit only after all seven entries, crossings, and the eight-letter extraction agree.
- Stop at 20 incorrect submissions, or earlier if no evidence distinguishes remaining candidates.
