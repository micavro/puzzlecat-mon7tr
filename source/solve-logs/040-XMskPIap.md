# Solve Log: Phigryptics

## Puzzle

- Puzzle ID: `XMskPIap`
- Owner: `codex-sub-b-20260712`
- Incorrect submissions before this solve: 0
- Final enumeration: `(6 2)`
- Theme: Phigros songs, chart designers, and a storyline character

## Reasoning

The notation on each clue is `extraction position / answer enumeration`, rather than a fractional value. The eight cryptic answers are all Phigros-related:

1. `SHADOW` (6): promotion `AD` interrupting `SHOW` -> `SH(AD)OW`; Shadow is a Phigros song. Extract 1 = `S`.
2. `ARENN SAKI` (5 4): anagram ("Anyhow") of `AS AN INKER`; Arenn Saki is a chart designer. Ignoring the space, extract 7 = `A`.
3. `SPOTLIGHT` (9): place `SPOT` + soft `LIGHT`; Spotlight is a Phigros song. Extract 4 = `T`.
4. `DEMIURGE` (8): half `DEMI` + desire `URGE`; Demiurge is a Phigros song. Extract 5 = `U`.
5. `CERERIS` (7): `CAREER IS` without `A`, rearranged by "unpleasant"; Cereris is a Phigros song. Extract 3 = `R`.
6. `NERSAN` (6): hidden among desig`NERS AN`d furries; NerSAN is a chart designer. Extract 1 = `N`.
7. `GINO` (4): hidden in ori`GIN O`riginal; Gino is a Phigros storyline character. Extract 4 = `O`.
8. `SALT` (4): `LAST` made "new" (anagram); Salt is a chart designer. Extract 1 = `S`.

The extracted letters are `SATURNOS`, matching the final enumeration as `SATURN OS`. This is independently reinforced by the puzzle's success message, "Welcome to Saturn OS!"

## Candidate Ranking

1. `SATURN OS` - exact extraction, exact `(6 2)` enumeration, and directly corroborated by the success-message theme.
2. No credible alternative: all eight component parses and extraction positions agree.

## Submissions

- Submission 1: `SATURN OS`
- Verdict: correct (`Welcome to Saturn OS!`)
- Incorrect count: 0

## Stopping Criteria

The uniquely extracted answer was accepted. Resolve as `correct` with incorrect count 0; no further submissions are needed.
